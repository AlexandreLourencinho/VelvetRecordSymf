<form method="post" action="{{ path('droits_delete', {'droitsId': droit.droitsId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ droit.droitsId) }}">
    <button class="btn">Delete</button>
</form>

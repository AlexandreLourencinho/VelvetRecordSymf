{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ArtistId</th>
                <td>{{ artist.artistId }}</td>
            </tr>
            <tr>
                <th>ArtistName</th>
                <td>{{ artist.artistName }}</td>
            </tr>
            <tr>
                <th>ArtistUrl</th>
                <td>{{ artist.artistUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artist_index') }}">back to list</a>

    <a href="{{ path('artist_edit', {'artistId': artist.artistId}) }}">edit</a>

    {{ include('artist/_delete_form.html.twig') }}
{% endblock %}
